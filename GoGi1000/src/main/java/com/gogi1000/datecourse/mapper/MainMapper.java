package com.gogi1000.datecourse.mapper;

import org.apache.ibatis.annotations.Mapper;

// DAO 역할을 해서 DAO 파일 만들 필요 없음
// main-mapper.xml로 연결해서 DB로 들어갔다가 값들 받아서 나옴.
@Mapper
public interface MainMapper {
	
}
